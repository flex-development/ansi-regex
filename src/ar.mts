/**
 * @file ar
 * @module ansi-regex/ar
 */

import ansiRegex from '#lib/ansi-regex'

/**
 * The default regular expression matching ANSI escape codes.
 *
 * @const {RegExp} regex
 */
const ar: RegExp = ansiRegex()

export default ar
